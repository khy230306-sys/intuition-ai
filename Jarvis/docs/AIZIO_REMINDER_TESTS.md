# Reminder / Relationship tests

Automated: `src/relationship/relationship.test.ts`, `src/smartReminder/smartReminder.test.ts`.

Covered: name parse, store/recall/delete, appointment create, past time, advance follow-up, cancel, person schedule, `think()` routing.

## Device (미확인 unless run on hardware)

- Notification permission grant/deny UX
- Alarm while app foreground
- Closed-app push for personal reminders (**not implemented**)
