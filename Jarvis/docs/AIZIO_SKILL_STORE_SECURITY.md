# Skill Store Security

**Status:** Manifest + builtin catalog 기반

- No remote code download/eval/install
- `validateManifest` + `assertNoRemoteCodeInstall`
- Future marketplace upload/payment = **향후 작업**
