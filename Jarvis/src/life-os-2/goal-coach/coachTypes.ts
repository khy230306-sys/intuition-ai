export type CoachAdvice = {
  goalTitle: string
  status: string
  progress: number
  progressBasis: string
  nextActions: string[]
  stallReason: string | null
  weekPlan: string[]
  warnings: string[]
}
