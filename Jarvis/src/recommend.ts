/**
 * @deprecated Import from `./stockEngine` — kept as a thin compatibility shim.
 */
export {
  REC_UNIVERSE,
  wantsStockRecommend,
  buildColdRecommendations,
  scorePick,
  detectMarket,
  type RecCandidate,
  type RecMarket,
  type ScoredPick,
} from './stockEngine'
