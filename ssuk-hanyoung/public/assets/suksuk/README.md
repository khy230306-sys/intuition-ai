# Suksuk Premium Illustration Drop Folder

Drop approved WebP files here using the filenames in `docs/ILLUSTRATION_PACK_V3.md`.
The registry auto-prefers these paths when status is REAL.

Do not commit crude generated art as REAL.
