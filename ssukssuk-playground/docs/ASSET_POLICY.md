# Asset Policy

**Superseded in authority by** [`VISUAL_ASSET_CONSTITUTION.md`](./VISUAL_ASSET_CONSTITUTION.md).

Quick pointers:

- `REFERENCE ≠ ASSET`
- Missing art → `ASSET_REQUIRED` with full specification (never temp SVG / emoji / crop)
- First baseline lock: 쑥쑥이 + `FIRE_TRUCK_01` + 자동차 공방
- Resolve all graphics via `src/assets/registry/`
